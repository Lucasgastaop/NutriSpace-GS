# Perfil ORACLE — banco FIAP (padrão da aplicação)

spring.datasource.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL
spring.datasource.username=rm563960
spring.datasource.password=020607
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# FIAP limita SESSIONS_PER_USER — Render: HIKARI_MAX_POOL_SIZE=1; local: 2 evita timeout com Postman
spring.datasource.hikari.maximum-pool-size=${HIKARI_MAX_POOL_SIZE:2}
spring.datasource.hikari.minimum-idle=0
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.leak-detection-threshold=60000
spring.datasource.hikari.idle-timeout=120000
spring.datasource.hikari.max-lifetime=180000

spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

spring.h2.console.enabled=false
